export type BookListProps = {
    id: number,
    _id: string,
    className: string,
    file: string,
    
}

export type NoticeProps = {
    _id: string,
    title: string,
    file: string,
    
}